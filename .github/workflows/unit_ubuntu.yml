############################################################
# -*- coding: utf-8 -*-
#
#       #   #  #   #   #    #
#      ##  ##  #  ##  #    #
#     # # # #  # # # #    #  #
#    #  ##  #  ##  ##    ######
#   #   #   #  #   #       #
#
# Python-based Tool for interaction with the 10micron mounts
# GUI with PySide6 for python3
#
# written in python3, (c) 2019-2025 by mworion
# Licence APL2.0
#
###########################################################

name: unit_ubuntu
concurrency:
  group: "${{ github.ref }}-${{ github.workflow }}"
  cancel-in-progress: true

on:
  push:
    # Run this action on the trigger event when specific files are touched
    paths:
      - 'src/mw4/**'
      - 'tests/unit_tests/**'
      - 'pyproject.toml'
      - '.github/workflows/unit_ubuntu.yml'
      - '.github/actions/install_ubuntu_head/action.yml'
      - '.github/actions/build_package/action.yml'
      - '.github/actions/setup_test/action.yml'

jobs:
  build_package:
    #
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      
      - name: build package
        uses: ./.github/actions/build_package
        
  run_tests_ubuntu_1:
    #
    needs: build_package
    runs-on: ubuntu-22.04
    
    steps:
      - uses: actions/checkout@v4
     
      - name: install head
        uses: ./.github/actions/install_ubuntu_head
    
      - name: install package
        uses: ./.github/actions/setup_test_cov

      - name: running_unit_tests
        run: |
          xvfb-run pytest tests/unit_tests/base -e /dev/stdout
          xvfb-run pytest tests/unit_tests/logic/* -e /dev/stdout
          xvfb-run pytest tests/unit_tests/mountcontrol -e /dev/stdout
          xvfb-run pytest tests/unit_tests/indibase -e /dev/stdout
          xvfb-run pytest tests/unit_tests/gui/utilities -e /dev/stdout
          xvfb-run pytest tests/unit_tests/gui/styles -e /dev/stdout
        
  run_tests_ubuntu_2:
    #
    needs: build_package
    runs-on: ubuntu-22.04
    
    steps:
      - uses: actions/checkout@v4
    
      - name: install package
        uses: ./.github/actions/setup_test
    
      - name: install head
        uses: ./.github/actions/install_ubuntu_head
  
      - name: running_unit_tests
        run: |
          xvfb-run pytest tests/unit_tests/gui/extWindows/video -e /dev/stdout
          xvfb-run pytest tests/unit_tests/gui/extWindows/image -e /dev/stdout
          xvfb-run pytest tests/unit_tests/gui/extWindows/hemisphere -e /dev/stdout
          xvfb-run pytest tests/unit_tests/gui/extWindows/simulator -e /dev/stdout

  run_tests_ubuntu_3:
    #
    needs: build_package
    runs-on: ubuntu-22.04
    
    steps:
      - uses: actions/checkout@v4
      
      - name: install package
        uses: ./.github/actions/setup_test
      
      - name: install head
        uses: ./.github/actions/install_ubuntu_head
      
      - name: running_unit_tests
        run: |
          xvfb-run pytest tests/unit_tests/gui/mainWindow -e /dev/stdout
          xvfb-run pytest tests/unit_tests/gui/mainWaddon -e /dev/stdout
          xvfb-run pytest tests/unit_tests/gui/extWindows -e /dev/stdout
        
  run_tests_ubuntu_4:
    #
    needs: build_package
    runs-on: ubuntu-22.04
    
    steps:
      - uses: actions/checkout@v4
    
      - name: install package
        uses: ./.github/actions/setup_test
    
      - name: install head
        uses: ./.github/actions/install_ubuntu_head
  
      - name: running_unit_tests
        run: |
          xvfb-run pytest tests/unit_tests/mainApp -e /dev/stdout
